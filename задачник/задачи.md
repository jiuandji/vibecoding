# ✅ Задачник: реализация practice-4

Сквозной список конкретных задач с чекбоксами. Опирается на `ТЗ/этапы.md`, но разбивает этапы на атомарные шаги.

**Как использовать:** отмечай `[x]` при завершении каждой задачи. Одна задача = 15-60 минут работы.

---

## Этап 0: Подготовка

- [ ] Создать папку `practice-4/` в корне
- [ ] Создать файлы: `index.html`, `style.css`, `script.js`
- [ ] Создать подпапку `practice-4/gallery/` для изображений
- [ ] В `practice-4/` создать `README.md` по шаблону practice-3
- [ ] Подключить reset.css или normalize в `style.css`

---

## Этап 1: Фундамент (статичный каркас)

### CSS-токены
- [ ] Скопировать палитру из `дизайн/визуальный-язык.md` в `:root`
- [ ] Добавить spacing scale (`--space-1` ... `--space-32`)
- [ ] Добавить font-size токены с `clamp()`
- [ ] Добавить radius tokens (`--radius-none`, `--radius-xs`, `--radius-sm`, `--radius-md`)
- [ ] Добавить duration и easing токены из `дизайн/движение.md`
- [ ] Добавить glow переменные (`--glow-nebula`, `--glow-plasma`, `--glow-soft`)

### Шрифты
- [ ] Подключить Google Fonts: Space Grotesk (400, 500, 600, 700)
- [ ] Подключить JetBrains Mono (400, 500)
- [ ] Задать `font-family` для body, headings, `.mono`

### HTML-структура (без стилей)
- [ ] `<header>` с навигацией и логотипом
- [ ] Секция Hero (`<section id="hero">`)
- [ ] Секция Missions (`<section id="missions">`)
- [ ] Секция Timeline (`<section id="timeline">`)
- [ ] Секция Data (`<section id="data">`)
- [ ] Секция Launch Control / форма (`<section id="launch">`)
- [ ] `<footer>` с 4 колонками

### Базовая стилизация
- [ ] Фон страницы `--color-void`
- [ ] Container: max-width 1280px + адаптивный padding
- [ ] Типографика: размеры H1-H3, body из токенов
- [ ] Вертикальные отступы между секциями
- [ ] Film grain overlay (SVG feTurbulence) как fixed `::after`

### Адаптивность
- [ ] Media query для mobile (≤640px)
- [ ] Media query для tablet (641-1024px)
- [ ] Проверить: нет горизонтального скролла ни на одном брейкпоинте

---

## Этап 2: Three.js — Starfield

- [ ] Подключить Three.js через CDN в `index.html`
- [ ] В `script.js`: создать класс `StarField`
  - [ ] Конструктор: принимает контейнер
  - [ ] Метод `init()`: Scene + Camera + Renderer
  - [ ] Метод `createStars()`: BufferGeometry + Points
  - [ ] Метод `animate()`: requestAnimationFrame цикл
- [ ] Подключить starfield в hero-секцию
- [ ] Обработчик `resize` → обновление камеры и рендерера
- [ ] Parallax: класс `StarField` слушает `mousemove`, двигает камеру с smooth lerp

---

## Этап 3: Интерактив UI

### Custom Cursor
- [ ] Класс `CustomCursor`
- [ ] Два div'а: кольцо + точка
- [ ] Следование за мышью с lerp
- [ ] Detect touch — если есть, скрыть курсор
- [ ] Hover-state на `[data-cursor="hover"]` элементах

### Magnetic Buttons
- [ ] Добавить атрибут `data-magnetic` на CTA-кнопки
- [ ] Класс `MagneticButton`
- [ ] GSAP: `mousemove` → смещение, `mouseleave` → elastic возврат

### Scroll Reveal
- [ ] Утилита `scrollReveal` на базе IntersectionObserver
- [ ] CSS-класс `.reveal` (opacity 0 + translateY 40px)
- [ ] При пересечении — добавить `.visible`
- [ ] Stagger delay для карточек через `style.transitionDelay`

### Page Loader
- [ ] HTML разметка loader'а (div.loader + текст + progress bar)
- [ ] CSS: fullscreen overlay, fade-out
- [ ] JS: скрывать после инициализации Three.js (минимум 800ms)

### Mobile Menu
- [ ] Burger-иконка (hamburger)
- [ ] Fullscreen overlay при клике
- [ ] Закрытие по крестику или по клику на ссылку

---

## Этап 4: Расширенный 3D

### Mission Cards
- [ ] Класс `MissionCard` (принимает контейнер + config)
- [ ] 3D-модель: примитив (IcosahedronGeometry или TorusGeometry) + MeshStandardMaterial
- [ ] Освещение: AmbientLight + PointLight
- [ ] Hover: запуск rotation (через GSAP tween или изменение state)
- [ ] Создать 3-6 карточек миссий с разными параметрами

### Timeline
- [ ] HTML: список точек timeline с годами и названиями
- [ ] CSS: горизонтальная ось, позиционирование точек
- [ ] JS: класс `TimelineCamera`
  - [ ] Scroll progress → camera.position.z
  - [ ] Подсветка активной точки
  - [ ] Плавная интерполяция позиции
- [ ] Интеграция с общей Three.js-сценой ИЛИ отдельная сцена для Timeline

### Оптимизация mobile
- [ ] Detect `window.innerWidth < 768` или touch-device
- [ ] Снизить количество звёзд в starfield (с 2000 до 600)
- [ ] Отключить custom cursor
- [ ] Упростить 3D-материалы (MeshBasicMaterial вместо Standard)

---

## Этап 5: Формы и счётчики

### Counter Animation
- [ ] Класс `CounterAnimator`
- [ ] Атрибут `data-counter="412"` на `<span>`
- [ ] При входе в viewport: анимация от 0 до target через RAF
- [ ] Easing: ease-out-quart (мануально через progress^0.5 или lib)

### Form
- [ ] HTML формы: input[name], input[email], select[mission], textarea[bio]
- [ ] CSS: floating labels, focus state с underline glow
- [ ] Класс `FormHandler`:
  - [ ] Валидация name (≥ 2 символа)
  - [ ] Валидация email (regex)
  - [ ] Submit: показать success-state
  - [ ] Сброс ошибок при изменении поля

### Success Animation
- [ ] Fade out полей (200ms)
- [ ] Fade in success-экрана (400ms, текст "TRANSMITTED")
- [ ] Опционально: частицы через Three.js (effects-scene)

---

## Этап 6: Полишинг

### Контент
- [ ] Сгенерировать hero-изображение (опциональный слой)
- [ ] Сгенерировать mission-изображения (6 штук)
- [ ] Заполнить все тексты реальными формулировками (не lorem)
- [ ] Добавить meta-теги (title, description, og:image, og:title)

### Производительность
- [ ] Lighthouse аудит → Performance ≥ 80
- [ ] Добавить `will-change: transform` на анимируемые элементы
- [ ] Проверить 60 FPS в Chrome DevTools > Performance
- [ ] Проверить размер JS-бандла ≤ 800KB

### Доступность
- [ ] Все `<img>` имеют `alt`
- [ ] Все form-fields имеют `<label>`
- [ ] Focus-состояния видимы
- [ ] WCAG AA контраст проверен
- [ ] `prefers-reduced-motion` отключает parallax, scramble, magnetic

### Устойчивость
- [ ] WebGL fallback: если `!canvas.getContext('webgl')` — статичный фон
- [ ] Ресайз не ломает 3D-сцену
- [ ] Mobile smoke test (реальное устройство или эмулятор)
- [ ] Console: 0 ошибок, 0 warnings

### Документация
- [ ] `practice-4/README.md` заполнен по шаблону practice-3
- [ ] Корневой `README.md` обновлён: секция про practice-4
- [ ] Структура проекта в корневом `README.md` включает practice-4

---

## Финальная проверка (gate)

- [ ] Пройдены все пункты `ТЗ/критерии-приемки.md`
- [ ] Визуал соответствует `дизайн/*.md`
- [ ] Все этапы отмечены выше

**Когда все чекбоксы выше отмечены — проект готов.**

---

## Метрики прогресса

Можно поставить счётчик внизу, чтобы видеть прогресс в целом.

```
Этап 0 (подготовка):        [ ] 0/5
Этап 1 (фундамент):         [ ] 0/19
Этап 2 (starfield):         [ ] 0/8
Этап 3 (UI интерактив):     [ ] 0/14
Этап 4 (3D расширенный):    [ ] 0/14
Этап 5 (формы, счётчики):   [ ] 0/11
Этап 6 (полишинг):          [ ] 0/19
```

Всего: **90 задач**.
